import { AntDesign } from "@expo/vector-icons";
import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Staro = ({ ...prp }) => {
  return <AntDesign name="staro" {...prp} />;
};

export default Staro;

const styles = StyleSheet.create({});
