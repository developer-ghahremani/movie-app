import { Ionicons } from "@expo/vector-icons";
import React from "react";
import { StyleSheet, Text, View } from "react-native";

const DownICon = ({ ...prp }) => {
  return <Ionicons name="chevron-down" {...prp} />;
};

export default DownICon;

const styles = StyleSheet.create({});
